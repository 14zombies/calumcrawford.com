---
##
domain: &domain https://calumcrawford.com
hugo_version: &hugo_version 0.111.3
## 
kind: pipeline
type: docker
name: main

steps:
- name: Clone submodules
  image: alpine/git
  commands:
  - git submodule update --init --recursive
    
- name: Hugo build
  image: plugins/hugo
  settings:
    hugo_version: *hugo_version
    minify: true
    pull: always
    validate: false
    url: *domain


- name: Docker image build & push
  image: plugins/docker
  settings:
    registry: git.14zombies.com
    username:
      from_secret: username
    password:
        from_secret: password
    repo: git.14zombies.com/cal/calumcrawford.com
    tags: 
      - latest
      - ${DRONE_TAG##v}

- name: Deploy
  image: plugins/webhook
  settings:
    urls:
      from_secret: hook

trigger:
  event:
    - tag

---
kind: signature
hmac: e08bbce674eb50da7ffea21b0ed4650f2030187d309fb79f4a9aa52a4dc87a2f

...
